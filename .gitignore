# --- Security: Private & Public Keys ---
# Never commit private keys
*.rsa
# Generally don't commit local public keys either (generated on the fly in CI)
*.rsa.pub
# Ignore the alpine keys you downloaded locally
alpine-devel@*.rsa.pub

# --- Melange: Build Artifacts ---
# The directory where .apk packages are generated
packages/

# --- Apko: Build Artifacts ---
# The resulting OCI image tarballs
output.tar
*.tar

# --- SBOMs (Software Bill of Materials) ---
sbom-*.json
*.spdx.json

# --- C Compilation Artifacts ---
# The binary generated by 'make' (from hello.c)
hello

# --- Scan Results ---
# If you run trivy locally
*.sarif

# --- OS / Editor ---
.DS_Store
.vscode/
!/.vscode/extensions.json
!/.vscode/settings.json